# Minimal local configuration for running via docker-compose (no hosted Supabase).

NODE_ENV=development

# Local auth bypass (shared with dashboard)
MIDDAY_AUTH_BYPASS=true
MIDDAY_AUTH_BYPASS_TOKEN=midday-local-dev-token
MIDDAY_AUTH_BYPASS_USER_ID=11111111-1111-1111-1111-111111111111
MIDDAY_AUTH_BYPASS_EMAIL=dev@midday.local
MIDDAY_AUTH_BYPASS_FULL_NAME=Local Dev

# Still required by some code paths; not used for auth bypass validation.
SUPABASE_JWT_SECRET=local-jwt-secret-not-used-with-bypass
SUPABASE_SERVICE_KEY=local-service-key-not-used-with-bypass
SUPABASE_URL=http://localhost:54321

# Optional: set to any non-empty string if you hit crypto-related runtime errors.
MIDDAY_ENCRYPTION_KEY=local-encryption-key-change-me

# MinIO storage (configured via docker-compose environment block)
USE_MINIO=true

